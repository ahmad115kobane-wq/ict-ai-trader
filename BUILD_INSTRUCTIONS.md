# 📱 دليل بناء التطبيق للإنتاج

## المتطلبات الأساسية

1. ✅ حساب Expo (مسجل بالفعل)
2. ✅ EAS CLI مثبت
3. ✅ ملف google-services.json موجود
4. ✅ ملف keystore موجود

## خطوات البناء

### 1. تسجيل الدخول إلى Expo

```bash
cd mobile
npx eas login
```

### 2. بناء APK للإنتاج (Android)

```bash
npx eas build --platform android --profile production
```

هذا الأمر سيقوم بـ:
- ✅ بناء APK موقع جاهز للنشر
- ✅ استخدام keystore الموجود
- ✅ تفعيل الإشعارات الكاملة
- ✅ تحسين الأداء للإنتاج

### 3. تحميل APK

بعد اكتمال البناء (يستغرق 10-15 دقيقة):
- سيظهر رابط لتحميل APK
- أو يمكنك تحميله من: https://expo.dev/accounts/[your-account]/projects/ict-ai-trader/builds

### 4. اختبار APK

1. حمّل APK على هاتفك
2. ثبّت التطبيق
3. اختبر جميع الميزات:
   - ✅ تسجيل الدخول
   - ✅ الإشعارات
   - ✅ التحليلات
   - ✅ الاشتراكات

### 5. النشر على Google Play Store (اختياري)

```bash
npx eas submit --platform android --profile production
```

## ملاحظات مهمة

### الإشعارات
- ✅ ستعمل الإشعارات بشكل كامل في APK الإنتاج
- ✅ تم تكوين Firebase بشكل صحيح
- ✅ تم إضافة جميع الأذونات المطلوبة

### الأداء
- ✅ التطبيق محسّن للإنتاج
- ✅ تم تفعيل New Architecture
- ✅ تم تحسين حجم APK

### الأمان
- ✅ التطبيق موقع بـ keystore
- ✅ جميع الاتصالات مشفرة (HTTPS)
- ✅ تم تأمين بيانات المستخدم

## الأوامر السريعة

### بناء سريع (APK فقط)
```bash
cd mobile
npx eas build --platform android --profile production --local
```

### التحقق من حالة البناء
```bash
npx eas build:list
```

### عرض معلومات المشروع
```bash
npx eas project:info
```

## استكشاف الأخطاء

### خطأ في keystore
```bash
# إعادة إنشاء keystore
keytool -genkeypair -v -keystore android/app/ict-ai-trader.keystore -alias ict-ai-trader -keyalg RSA -keysize 2048 -validity 10000
```

### خطأ في google-services.json
- تأكد من وجود الملف في: `mobile/google-services.json`
- تأكد من صحة محتوى الملف

### خطأ في البناء
```bash
# مسح الكاش وإعادة المحاولة
npx expo start --clear
npx eas build --platform android --profile production --clear-cache
```

## معلومات التطبيق

- **اسم التطبيق:** ICT AI Trader
- **Package Name:** com.ictaitrader.app
- **Version:** 1.0.0
- **Version Code:** 4
- **Project ID:** 1881cacc-0c4d-4a83-a05e-19f20a07f2c0

## الخطوات التالية بعد البناء

1. ✅ اختبار APK على أجهزة مختلفة
2. ✅ التأكد من عمل الإشعارات
3. ✅ اختبار جميع الميزات
4. ✅ جمع ملاحظات المستخدمين
5. ✅ النشر على Google Play Store

## الدعم

إذا واجهت أي مشاكل:
- راجع logs البناء في Expo Dashboard
- تحقق من ملفات الإعدادات
- تأكد من تحديث جميع المكتبات

---

**ملاحظة:** البناء الأول قد يستغرق وقتاً أطول. البناءات اللاحقة ستكون أسرع.
