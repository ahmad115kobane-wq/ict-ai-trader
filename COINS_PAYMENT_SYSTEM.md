# نظام الدفع بالعملات للباقات

## التغييرات المطبقة

### 1. تعديل الخادم (Server) ✅

#### في `server/src/routes/subscription.ts`:

**إضافة سعر العملات في API الباقات**:
```typescript
packages: packages.map(pkg => ({
  ...pkg,
  coinPrice: Math.round(pkg.price * 100), // 1 دولار = 100 عملة
}))
```

**تعديل endpoint الشراء**:
- حساب سعر الباقة بالعملات: `coinPrice = price * 100`
- التحقق من رصيد المستخدم
- خصم العملات قبل الشراء
- إرجاع العملات في حالة فشل الشراء
- تسجيل العملية في سجل المعاملات

**منطق الشراء**:
```typescript
1. حساب السعر بالعملات
2. التحقق من الرصيد الكافي
3. خصم العملات
4. تفعيل الاشتراك
5. إرجاع العملات إذا فشل التفعيل
```

### 2. تعديل التطبيق (Mobile) ✅

#### في `mobile/src/screens/SubscriptionScreen.tsx`:

**عرض السعر بالعملات والدولار**:
```typescript
<View style={styles.priceContainer}>
  <View style={styles.coinPriceRow}>
    <Ionicons name="diamond" size={16} color={colors.gold} />
    <Text style={styles.packagePrice}>{coinPrice}</Text>
  </View>
  <Text style={styles.dollarPrice}>(${pkg.price})</Text>
</View>
```

**تحديث دالة الشراء**:
- التحقق من الرصيد قبل الشراء
- عرض رسالة تأكيد مع تفاصيل الرصيد
- عرض رسالة خطأ إذا كان الرصيد غير كافٍ

**رسالة التأكيد**:
```
هل تريد شراء هذه الباقة مقابل {coinPrice} عملة؟

رصيدك الحالي: {userCoins} عملة
رصيدك بعد الشراء: {userCoins - coinPrice} عملة
```

### 3. معدل التحويل 💰

**1 دولار = 100 عملة**

أمثلة:
- باقة $5 = 500 عملة
- باقة $10 = 1000 عملة
- باقة $20 = 2000 عملة

### 4. الميزات الجديدة ✅

1. **عرض السعر بالعملات**: السعر الأساسي بالعملات مع أيقونة الماسة
2. **عرض المعادل بالدولار**: بين قوسين تحت السعر بالعملات
3. **التحقق من الرصيد**: قبل إتمام عملية الشراء
4. **رسائل واضحة**: توضح الرصيد الحالي والمطلوب
5. **حماية من الأخطاء**: إرجاع العملات في حالة فشل التفعيل

## النتيجة

✅ نظام دفع كامل بالعملات
✅ عرض السعر بالعملات والدولار معاً
✅ التحقق من الرصيد قبل الشراء
✅ رسائل واضحة ومفصلة
✅ حماية من الأخطاء والاحتيال

## الملفات المعدلة

- `server/src/routes/subscription.ts` - نظام الدفع بالعملات
- `mobile/src/screens/SubscriptionScreen.tsx` - واجهة عرض الباقات والشراء
