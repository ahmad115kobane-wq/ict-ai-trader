# إضافة زر الباقات في التليجرام

## التحديثات المطبقة

### 1. إضافة زر "الباقات" في القائمة الرئيسية ✅

في `handleStartCommand()`:
```typescript
const keyboard = {
  inline_keyboard: [
    [{ text: `${autoStatus} التحليل التلقائي`, callback_data: 'toggle_auto' }],
    [{ text: '📋 تفاصيل اشتراكي', callback_data: 'subscription_details' }],
    [{ text: '💎 الباقات', callback_data: 'show_packages' }],  // ← جديد
    [{ text: '📅 التقويم الاقتصادي', callback_data: 'economic_calendar' }],
    [{ text: '💬 الدعم الفني', url: 'https://t.me/iqbotict' }]
  ]
};
```

### 2. إضافة معالج الزر ✅

في callback handlers:
```typescript
else if (data === 'show_packages') {
  console.log(`💎 Showing packages for user ${user.id}`);
  await answerCallbackQuery(callbackQuery.id, '💎 عرض الباقات');
  await showPackages(chatId, user);
}
```

### 3. تحسين عرض الباقات ✅

في `showPackages()`:

**عرض الرصيد**:
```typescript
message += `💰 رصيدك الحالي: <b>${user.coins || 0} عملة</b>\n\n`;
```

**حساب السعر بالعملات**:
```typescript
const coinPrice = Math.round(pkg.price * 100); // 1 دولار = 100 عملة
message += `💎 السعر: <b>${coinPrice} عملة</b> ($${pkg.price})\n`;
```

**تحسين التنسيق**:
```
🎁 الباقات المتاحة

💰 رصيدك الحالي: 1000 عملة

━━━━━━━━━━━━━━━━━━

📦 الباقة الأسبوعية
💎 السعر: 500 عملة ($5)
⏰ المدة: 7 يوم
🎁 عملات مجانية: +100 عملة
📊 التحليلات: غير محدودة

━━━━━━━━━━━━━━━━━━
👇 اختر الباقة المناسبة لك:
```

**أزرار محسّنة**:
```typescript
text: `💎 ${pkg.name_ar} - ${coinPrice} عملة`
```

## الميزات الجديدة

1. ✅ زر "الباقات" في القائمة الرئيسية
2. ✅ عرض رصيد المستخدم الحالي
3. ✅ عرض السعر بالعملات والدولار
4. ✅ تنسيق جميل ومنظم
5. ✅ أزرار واضحة مع الأسعار

## كيفية الاستخدام

1. المستخدم يضغط على "💎 الباقات"
2. يظهر له:
   - رصيده الحالي
   - قائمة الباقات مع الأسعار بالعملات
   - المعادل بالدولار
   - العملات المجانية المضافة
3. يختار الباقة المناسبة
4. يتم الشراء بالعملات

## الملفات المعدلة

- `server/src/services/telegramBotService.ts`
  - إضافة زر الباقات في القائمة
  - إضافة معالج show_packages
  - تحسين دالة showPackages
