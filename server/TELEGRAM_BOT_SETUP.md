# 🤖 إعداد بوت تليجرام لإشارات التداول

## الخطوة 1: إنشاء بوت تليجرام

1. افتح تليجرام وابحث عن `@BotFather`
2. أرسل الأمر `/newbot`
3. اختر اسم للبوت (مثال: ICT AI Trader Bot)
4. اختر username للبوت (يجب أن ينتهي بـ bot، مثال: `ict_ai_trader_bot`)
5. ستحصل على **Bot Token** - احتفظ به

## الخطوة 2: الحصول على Chat ID

### طريقة 1: إنشاء قناة خاصة (موصى بها)

1. أنشئ قناة جديدة في تليجرام
2. اجعلها خاصة (Private)
3. أضف البوت كمسؤول (Admin) في القناة
4. أرسل رسالة في القناة
5. افتح الرابط التالي في المتصفح (استبدل YOUR_BOT_TOKEN بالتوكن الخاص بك):
   ```
   https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates
   ```
6. ابحث عن `"chat":{"id":-1001234567890` - هذا هو Chat ID الخاص بالقناة

### طريقة 2: استخدام محادثة شخصية

1. ابحث عن البوت الخاص بك في تليجرام
2. ابدأ محادثة معه بإرسال `/start`
3. افتح الرابط التالي في المتصفح:
   ```
   https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates
   ```
4. ابحث عن `"chat":{"id":123456789` - هذا هو Chat ID الخاص بك

## الخطوة 3: إضافة المتغيرات البيئية

أضف المتغيرات التالية إلى ملف `.env`:

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
```

**مثال:**
```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=-1001234567890
```

## الخطوة 4: إعادة تشغيل السيرفر

```bash
npm run dev
```

## الخطوة 5: اختبار البوت

سيرسل البوت إشارات تلقائياً عند:
- اكتشاف فرصة تداول جديدة (PLACE_PENDING)
- التحليل التلقائي كل 5 دقائق

## 📱 شكل الرسالة

```
🟢 إشارة شراء جديدة

📊 الزوج: XAUUSD
💰 الدخول: 2650.50
🎯 الهدف: 2665.00
🛑 الإيقاف: 2645.00

📈 نسبة RR: 1:2.90
✅ الثقة: 85%

⏰ الوقت: 2024-01-26 10:30:00

تم إنشاؤها بواسطة ICT AI Trader
```

## 🔧 التخصيص

يمكنك تعديل شكل الرسالة في ملف:
```
server/src/services/telegramService.ts
```

## ⚠️ ملاحظات مهمة

1. **الأمان**: لا تشارك Bot Token مع أحد
2. **القناة الخاصة**: استخدم قناة خاصة لحماية الإشارات
3. **الإشعارات**: يمكنك تعطيل الإشعارات لتحليلات "NO_TRADE" لتجنب الإزعاج
4. **التكرار**: البوت يرسل فقط عند اكتشاف صفقة جديدة (PLACE_PENDING)

## 🧪 اختبار البوت

لاختبار أن البوت يعمل، يمكنك إرسال رسالة اختبار:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/sendMessage" \
  -H "Content-Type: application/json" \
  -d '{"chat_id":"<YOUR_CHAT_ID>","text":"🤖 البوت يعمل بنجاح!"}'
```

## 📊 مثال على الاستخدام في الكود

```typescript
import { sendTradeSignal } from './services/telegramService';

// عند اكتشاف صفقة جديدة
if (analysis.decision === 'PLACE_PENDING' && analysis.suggestedTrade) {
  await sendTradeSignal(process.env.TELEGRAM_CHAT_ID!, {
    type: analysis.suggestedTrade.type.includes('BUY') ? 'BUY' : 'SELL',
    entry: analysis.suggestedTrade.entry,
    sl: analysis.suggestedTrade.sl,
    tp: analysis.suggestedTrade.tp,
    confidence: analysis.confidence,
    pair: 'XAUUSD',
    timestamp: new Date()
  });
}
```

## 🎯 الميزات

✅ إرسال إشارات الصفقات فقط (BUY/SELL)
✅ تنسيق عربي جميل
✅ معلومات كاملة (Entry, SL, TP, RR, Confidence)
✅ توقيت دقيق
✅ آمن ومشفر

## 🆘 المساعدة

إذا واجهت مشاكل:
1. تأكد من صحة Bot Token
2. تأكد من صحة Chat ID
3. تأكد من أن البوت مضاف كمسؤول في القناة
4. تحقق من logs السيرفر
