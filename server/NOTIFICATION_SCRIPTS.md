# سكريبتات إرسال الإشعارات

## نظرة عامة

تم إنشاء سكريبتات لإرسال إشعارات النظام للمستخدمين عبر التطبيق و Telegram.

## السكريبتات المتاحة

### 1. اختبار إشعار ترحيبي (test-welcome-notification.js)

يرسل إشعار ترحيبي لجميع المستخدمين في قاعدة البيانات.

**الاستخدام:**
```bash
cd server
npm run test:welcome
```

**ماذا يفعل:**
- يجلب جميع المستخدمين من قاعدة البيانات
- يرسل إشعار ترحيبي لكل مستخدم
- يعرض قائمة بالمستخدمين والحالة

---

### 2. إرسال إشعار مخصص (send-notification.js)

سكريبت متعدد الاستخدامات لإرسال أنواع مختلفة من الإشعارات.

#### أ) إرسال إشعار ترحيبي

**لجميع المستخدمين:**
```bash
cd server
npm run notify welcome
```

**لمستخدم محدد:**
```bash
npm run notify welcome user@example.com
```

**لمستخدم Telegram:**
```bash
npm run notify welcome telegram_123456@ict-trader.local
```

---

#### ب) إرسال تحديث نظام

**لجميع المستخدمين:**
```bash
npm run notify update "تم إضافة ميزات جديدة للتطبيق!"
```

**أمثلة:**
```bash
npm run notify update "تحديث مهم: تم تحسين أداء التطبيق"
npm run notify update "صيانة مجدولة: سيتم إيقاف الخدمة لمدة ساعة"
npm run notify update "ميزة جديدة: الآن يمكنك تتبع صفقاتك بشكل أفضل"
```

---

#### ج) إرسال إشعار مخصص

**لجميع المستخدمين:**
```bash
npm run notify custom "العنوان" "الرسالة"
```

**لمستخدم محدد:**
```bash
npm run notify custom "العنوان" "الرسالة" user@example.com
```

**أمثلة:**
```bash
# عرض خاص
npm run notify custom "🎉 عرض خاص" "خصم 50% على جميع الباقات لمدة 24 ساعة!"

# تنبيه مهم
npm run notify custom "⚠️ تنبيه مهم" "يرجى تحديث التطبيق إلى آخر إصدار"

# إعلان
npm run notify custom "📢 إعلان" "تم إطلاق ميزة التحليل التلقائي المحسّن"

# لمستخدم محدد
npm run notify custom "🎁 مكافأة خاصة" "تم إضافة 100 عملة إلى حسابك" user@example.com
```

---

## عرض المساعدة

للحصول على قائمة كاملة بالأوامر:
```bash
npm run notify
```

---

## أمثلة عملية

### مثال 1: إرسال ترحيب لجميع المستخدمين
```bash
cd server
npm run notify welcome
```

**النتيجة:**
- يرسل إشعار ترحيبي لكل مستخدم
- يظهر في تطبيق الهاتف
- يظهر في Telegram (للمستخدمين المسجلين عبر Telegram)

---

### مثال 2: إعلان عن عرض خاص
```bash
npm run notify custom "🎉 عرض محدود" "احصل على باقة Premium بنصف السعر! العرض ينتهي خلال 48 ساعة"
```

---

### مثال 3: تحديث مهم
```bash
npm run notify update "تم إصلاح مشكلة في نظام الإشعارات. يرجى إعادة تشغيل التطبيق"
```

---

### مثال 4: إشعار لمستخدم محدد
```bash
npm run notify custom "🏆 تهانينا" "لقد ربحت 500 عملة مجانية!" telegram_123456@ict-trader.local
```

---

## كيف يعمل النظام

### 1. قاعدة البيانات
- يتم حفظ الإشعار في جدول `system_notifications`
- يرتبط بمعرف المستخدم

### 2. Telegram
- إذا كان المستخدم مسجل عبر Telegram، يتم إرسال الإشعار فوراً
- يظهر مع أيقونات وأزرار تفاعلية

### 3. Push Notifications
- إذا كان لدى المستخدم Push Token، يتم إرسال إشعار فوري
- يظهر حتى لو كان التطبيق مغلقاً

### 4. التطبيق
- يظهر الإشعار في شاشة الإشعارات
- يمكن تعليمه كمقروء أو حذفه

---

## الإشعارات التلقائية

بالإضافة للإشعارات اليدوية، يرسل النظام إشعارات تلقائية:

### 1. انتهاء الاشتراك
- يتم إرساله تلقائياً عند انتهاء اشتراك المستخدم
- يحتوي على زر "تجديد الاشتراك"

### 2. قرب انتهاء الاشتراك
- يتم إرساله قبل 3 أيام من انتهاء الاشتراك
- يذكر المستخدم بتجديد الاشتراك

### 3. شراء اشتراك
- يتم إرساله فوراً عند شراء باقة جديدة
- يحتوي على تفاصيل الاشتراك

### 4. انخفاض العملات
- يتم إرساله عندما يقل رصيد المستخدم
- يحتوي على زر "شراء عملات"

---

## المهام الدورية

يتم فحص الاشتراكات تلقائياً كل 6 ساعات:
- البحث عن اشتراكات منتهية
- البحث عن اشتراكات قريبة من الانتهاء
- إرسال الإشعارات المناسبة

---

## استكشاف الأخطاء

### المشكلة: "Database not initialized"
**الحل:** تأكد من أن السيرفر يعمل أو قم بتشغيل:
```bash
npm run build
```

### المشكلة: "User not found"
**الحل:** تحقق من البريد الإلكتروني للمستخدم:
```bash
# في قاعدة البيانات
SELECT email FROM users;
```

### المشكلة: "No users found"
**الحل:** تأكد من وجود مستخدمين في قاعدة البيانات

---

## ملاحظات مهمة

1. **التوقيت:** يتم إرسال الإشعارات فوراً
2. **التكرار:** لا يتم إرسال نفس الإشعار مرتين
3. **الأولوية:** إشعارات الاشتراك لها أولوية عالية
4. **التخزين:** يتم حفظ جميع الإشعارات في قاعدة البيانات
5. **الحذف:** يمكن للمستخدم حذف الإشعارات من التطبيق

---

## أفضل الممارسات

### 1. العناوين
- استخدم عناوين قصيرة وواضحة
- أضف أيقونات emoji للفت الانتباه
- مثال: "🎉 عرض خاص" بدلاً من "عرض"

### 2. الرسائل
- اجعل الرسالة مختصرة ومفيدة
- استخدم لغة ودية
- أضف دعوة للعمل (Call to Action)

### 3. التوقيت
- أرسل الإشعارات في أوقات مناسبة
- تجنب الإرسال في وقت متأخر من الليل
- لا ترسل إشعارات كثيرة في وقت قصير

### 4. الاختبار
- اختبر الإشعار على مستخدم واحد أولاً
- تحقق من ظهوره في التطبيق و Telegram
- تأكد من صحة الروابط والأزرار

---

## أمثلة متقدمة

### إرسال إشعارات متعددة
```bash
# إشعار ترحيبي
npm run notify welcome

# انتظر قليلاً
sleep 2

# إشعار بعرض خاص
npm run notify custom "🎁 هدية ترحيبية" "احصل على 50 عملة مجانية!"
```

### إرسال لمستخدمين محددين
```bash
# قائمة المستخدمين
npm run notify custom "إعلان" "رسالة خاصة" user1@example.com
npm run notify custom "إعلان" "رسالة خاصة" user2@example.com
npm run notify custom "إعلان" "رسالة خاصة" user3@example.com
```

---

## الدعم

إذا واجهت أي مشاكل:
1. تحقق من ملف `.env` وتأكد من صحة المتغيرات
2. تأكد من أن قاعدة البيانات تعمل
3. راجع ملف `SYSTEM_NOTIFICATIONS_GUIDE.md` للمزيد من التفاصيل
