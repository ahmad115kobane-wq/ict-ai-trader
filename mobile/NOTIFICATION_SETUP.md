# 📱 إعداد نظام الإشعارات - ICT AI Trader

## ✅ Project ID
```
1881cacc-0c4d-4a83-a05e-19f20a07f2c0
```

---

## 🔧 متطلبات الإشعارات

### على التطبيق (Client):
1. ✅ `expo-notifications` مثبت
2. ✅ `expo-device` مثبت
3. ✅ `projectId` محدد في `app.json`
4. ✅ قنوات الإشعارات معدة لـ Android
5. ✅ صلاحيات الإشعارات مطلوبة من المستخدم

### على السيرفر (Server):
1. ✅ `expo-server-sdk` مثبت
2. ✅ نقطة نهاية `/api/auth/register-push-token` موجودة
3. ✅ دالة `sendTradeNotification` موجودة
4. ✅ التحليل التلقائي يرسل إشعارات عند اكتشاف فرصة

---

## 📋 شروط استلام الإشعارات

لكي يستلم المستخدم إشعارات الصفقات، يجب أن:

1. **يكون لديه اشتراك نشط** (ليس مجاني)
2. **يفعّل التحليل التلقائي** من التطبيق
3. **يمنح صلاحية الإشعارات** للتطبيق
4. **يكون Push Token مسجل** في السيرفر

---

## 🧪 اختبار الإشعارات

### من السيرفر:
```bash
# إرسال إشعار تجريبي لمستخدم محدد
curl "https://ict-ai-trader-production.up.railway.app/send-test-notification?email=your@email.com"

# إنشاء صفقة تجريبية وإرسال إشعارات لجميع المستخدمين
curl "https://ict-ai-trader-production.up.railway.app/send-test-trade"

# التحقق من حالة المستخدمين
curl "https://ict-ai-trader-production.up.railway.app/debug-users"

# التحقق من حالة الإشعارات
curl "https://ict-ai-trader-production.up.railway.app/debug-notifications"
```

---

## 📊 تدفق الإشعارات

```
┌─────────────────┐
│  السيرفر        │
│  (كل 5 دقائق)   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  تحليل السوق   │
│  analyzeMulti   │
└────────┬────────┘
         │
         ▼
┌─────────────────────┐
│  هل توجد فرصة؟      │
│  PLACE_PENDING?     │
└──────┬──────────────┘
       │ نعم
       ▼
┌─────────────────────┐
│  جلب المستخدمين     │
│  getUsersWithPush   │
│  Tokens             │
└────────┬────────────┘
         │
         ▼
┌─────────────────────┐
│  إرسال الإشعارات   │
│  sendTradeNotif     │
│  (Expo Push)        │
└────────┬────────────┘
         │
         ▼
┌─────────────────┐
│  التطبيق        │
│  يستلم الإشعار  │
└─────────────────┘
```

---

## 🔍 تشخيص المشاكل

### الإشعارات لا تصل:

1. **تحقق من الصلاحيات:**
   ```javascript
   const { status } = await Notifications.getPermissionsAsync();
   console.log('Permission status:', status);
   ```

2. **تحقق من Push Token:**
   - افتح التطبيق وشاهد Console للتأكد من ظهور:
   ```
   ✅ Push token obtained: ExponentPushToken[...]
   ✅ Push token registered with server
   ```

3. **تحقق من السيرفر:**
   ```bash
   curl "https://ict-ai-trader-production.up.railway.app/debug-notifications"
   ```

4. **تحقق من الاشتراك:**
   - يجب أن يكون الاشتراك نشط (غير مجاني ولم ينتهِ)

5. **تحقق من التحليل التلقائي:**
   - يجب أن يكون `auto_analysis_enabled = true`

---

## 📱 قنوات الإشعارات (Android)

### القناة الرئيسية: `trade-alerts-v2`
- **الأهمية:** MAX
- **الاهتزاز:** نعم
- **الضوء:** أخضر (#10b981)
- **الصوت:** default
- **تجاوز وضع عدم الإزعاج:** نعم
- **الظهور على شاشة القفل:** عام

---

## 📝 ملاحظات مهمة

1. الإشعارات تعمل فقط على الأجهزة الحقيقية (ليس المحاكي)
2. يجب إعادة بناء التطبيق بعد تغيير `app.json`
3. على iOS، يجب تفعيل Push Notifications في Xcode
4. تأكد من أن التطبيق ليس في وضع Battery Optimization على Android
