# 🧪 دليل اختبار الإشعارات

## 📱 الاختبار على الجهاز

### 1. بناء APK جديد
```bash
cd mobile
eas build --platform android --profile production
```

### 2. تثبيت APK على جهاز حقيقي
- قم بتنزيل APK من EAS Build
- ثبته على جهاز Android حقيقي (ليس محاكي)

### 3. تسجيل الدخول
- افتح التطبيق
- سجل دخول بحسابك
- انتظر رسالة "Push token registered with server"

### 4. التحقق من تسجيل Token
افتح المتصفح واذهب إلى:
```
https://ict-ai-trader-production.up.railway.app/api/auth/list-push-tokens
```

يجب أن ترى push token الخاص بك في القائمة.

### 5. اختبار إرسال إشعار

#### الطريقة 1: من Expo Push Tool
1. اذهب إلى: https://expo.dev/notifications
2. الصق push token الخاص بك
3. اكتب عنوان ورسالة
4. اضغط "Send a Notification"

#### الطريقة 2: من السيرفر (عند تحليل تلقائي)
- فعّل التحليل التلقائي من التطبيق
- انتظر 5 دقائق (عند إغلاق شمعة M5)
- يجب أن يصلك إشعار إذا وجدت فرصة

#### الطريقة 3: اختبار يدوي من السيرفر
استخدم هذا الكود في Node.js:

```javascript
const { Expo } = require('expo-server-sdk');
const expo = new Expo();

const pushToken = 'ExponentPushToken[YOUR_TOKEN_HERE]';

const message = {
  to: pushToken,
  sound: 'default',
  title: '🧪 اختبار الإشعارات',
  body: 'هذا إشعار تجريبي من السيرفر',
  data: { test: true },
  priority: 'high',
  channelId: 'trade-alerts-v2',
};

expo.sendPushNotificationsAsync([message])
  .then(tickets => {
    console.log('✅ Notification sent:', tickets);
  })
  .catch(error => {
    console.error('❌ Error:', error);
  });
```

---

## 🔍 فحص Logs

### على Android (باستخدام ADB)
```bash
# تشغيل logcat
adb logcat | grep -i "expo\|notification\|push\|ICT"

# أو حفظ في ملف
adb logcat > logs.txt
```

### في التطبيق
افتح التطبيق وراقب Console logs في Metro bundler.

---

## ✅ Checklist

- [ ] تم بناء APK جديد بعد التعديلات
- [ ] تم تثبيت APK على جهاز حقيقي
- [ ] تم تسجيل الدخول بنجاح
- [ ] تم الحصول على Push Token
- [ ] تم تسجيل Token في السيرفر
- [ ] تم اختبار إرسال إشعار من Expo Tool
- [ ] الإشعار وصل بنجاح
- [ ] الإشعار يظهر عند إغلاق التطبيق
- [ ] الإشعار يظهر عند قفل الشاشة
- [ ] النقر على الإشعار يفتح التطبيق

---

## 🐛 استكشاف الأخطاء

### المشكلة: لا يتم الحصول على Push Token
**الحل**:
1. تأكد من أن الجهاز متصل بالإنترنت
2. تأكد من أن الأذونات ممنوحة
3. تحقق من Firebase Console
4. أعد تثبيت التطبيق

### المشكلة: Token يتم تسجيله لكن الإشعارات لا تصل
**الحل**:
1. تحقق من Firebase Cloud Messaging في Console
2. تأكد من أن `google-services.json` صحيح
3. تحقق من أن السيرفر يرسل الإشعارات (راجع Railway logs)
4. اختبر باستخدام Expo Push Tool

### المشكلة: الإشعارات تصل لكن لا تظهر
**الحل**:
1. تحقق من إعدادات الإشعارات في الجهاز
2. تأكد من أن التطبيق لديه أذونات الإشعارات
3. تحقق من Battery Optimization settings
4. بعض الأجهزة (Xiaomi, Huawei) تحتاج إعدادات خاصة

### المشكلة: الإشعارات تعمل في Foreground فقط
**الحل**:
1. تأكد من أن `channelId` صحيح
2. تحقق من أن `priority` مضبوط على `high`
3. تأكد من أن Battery Optimization معطل للتطبيق

---

## 📊 معلومات مفيدة

### Push Token Format
```
ExponentPushToken[xxxxxxxxxxxxxxxxxxxxxx]
```

### Notification Channels
- `trade-alerts-v2`: للإشعارات المهمة (صفقات)
- `general`: للإشعارات العامة

### Priority Levels
- `default`: عادي
- `normal`: عادي
- `high`: عالي (يظهر حتى في Do Not Disturb)

---

## 🎯 النتيجة المتوقعة

بعد تطبيق جميع الإصلاحات:
- ✅ الإشعارات تعمل في Expo Go
- ✅ الإشعارات تعمل في Production Build
- ✅ الإشعارات تظهر عند إغلاق التطبيق
- ✅ الإشعارات تظهر عند قفل الشاشة
- ✅ الإشعارات تصدر صوت
- ✅ النقر على الإشعار يفتح التطبيق
