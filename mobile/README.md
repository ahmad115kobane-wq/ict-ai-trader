# ICT AI Trader - تطبيق الهاتف

تطبيق React Native مع Expo SDK 52 للتداول بالذكاء الاصطناعي.

## 🚀 البدء السريع

### 1. تثبيت المتطلبات

```bash
# التأكد من تثبيت Node.js (الإصدار 18+)
node --version

# تثبيت Expo CLI
npm install -g expo-cli

# تثبيت EAS CLI للبناء
npm install -g eas-cli
```

### 2. تثبيت التبعيات

```bash
cd mobile
npm install
```

### 3. إعداد السيرفر

افتح الملف `src/config/api.ts` وغير عنوان السيرفر:

```typescript
export const API_BASE_URL = 'https://your-server.railway.app';
```

### 4. تشغيل التطبيق في وضع التطوير

```bash
# تشغيل Expo
npx expo start

# أو مباشرة على Android
npx expo run:android

# أو على iOS
npx expo run:ios
```

## 📱 بناء ملف APK

### الطريقة 1: باستخدام EAS Build (موصى بها)

1. **تسجيل الدخول لـ Expo:**
```bash
eas login
```

2. **إعداد المشروع:**
```bash
eas build:configure
```

3. **بناء APK:**
```bash
eas build --platform android --profile preview
```

سيتم رفع التطبيق للبناء في سيرفرات Expo وستحصل على رابط تحميل APK.

### الطريقة 2: بناء محلي

```bash
# تثبيت التبعيات للبناء المحلي
npx expo install expo-dev-client

# البناء
npx expo run:android --variant release
```

## 🔧 الإعدادات

### تكوين app.json

غير الإعدادات التالية حسب حاجتك:

- `expo.name`: اسم التطبيق
- `expo.android.package`: معرف الحزمة (com.yourcompany.app)
- `expo.extra.eas.projectId`: معرف مشروع Expo (اختياري)

### إعدادات الإشعارات

1. أنشئ حساب على [Expo](https://expo.dev)
2. احصل على معرف المشروع
3. أضفه في `app.json`:

```json
{
  "expo": {
    "extra": {
      "eas": {
        "projectId": "your-project-id"
      }
    }
  }
}
```

## 📁 هيكل المشروع

```
mobile/
├── App.tsx              # نقطة الدخول الرئيسية
├── index.js             # ملف التسجيل
├── app.json             # إعدادات Expo
├── package.json         # التبعيات
├── eas.json            # إعدادات EAS Build
├── assets/              # الأصول (صور، أيقونات)
└── src/
    ├── components/      # المكونات القابلة لإعادة الاستخدام
    │   ├── Header.tsx
    │   └── TradeCard.tsx
    ├── config/
    │   └── api.ts       # إعدادات API
    ├── context/
    │   └── AuthContext.tsx
    ├── navigation/
    │   └── AppNavigator.tsx
    ├── screens/
    │   ├── HomeScreen.tsx
    │   ├── LoginScreen.tsx
    │   ├── RegisterScreen.tsx
    │   ├── TradesScreen.tsx
    │   └── SubscriptionScreen.tsx
    ├── services/
    │   ├── apiService.ts
    │   └── notificationService.ts
    ├── theme/
    │   └── index.ts
    └── types/
        └── index.ts
```

## ✨ الميزات

- ✅ تسجيل الدخول وإنشاء الحساب
- ✅ عرض الشارت المباشر
- ✅ استقبال التحليلات التلقائية
- ✅ سجل الصفقات مع نسخ القيم
- ✅ إدارة الاشتراكات
- ✅ إشعارات Push للصفقات الجديدة
- ✅ واجهة عربية بالكامل
- ✅ وضع داكن (Dark Mode)

## 🔗 الاتصال بالسيرفر

التطبيق يتصل بالسيرفر عبر REST API:

| Endpoint | الوظيفة |
|----------|---------|
| `/api/auth/login` | تسجيل الدخول |
| `/api/auth/register` | إنشاء حساب |
| `/api/auth/me` | بيانات المستخدم |
| `/api/analysis/latest-auto` | آخر تحليل |
| `/api/analysis/trades-history` | سجل الصفقات |
| `/api/subscription/packages` | الباقات المتاحة |
| `/api/subscription/status` | حالة الاشتراك |

## 🐛 استكشاف الأخطاء

### خطأ في الاتصال بالسيرفر

- تأكد من صحة `API_BASE_URL` في `src/config/api.ts`
- تأكد من أن السيرفر يعمل
- تأكد من إعدادات CORS في السيرفر

### الإشعارات لا تعمل

- تأكد من استخدام جهاز حقيقي (ليس محاكي)
- تأكد من منح صلاحيات الإشعارات
- تأكد من تسجيل Push Token في السيرفر

### خطأ في البناء

```bash
# مسح الكاش
npx expo start -c

# إعادة تثبيت التبعيات
rm -rf node_modules
npm install
```

## 📞 الدعم

إذا واجهت أي مشكلة، تأكد من:
1. تحديث جميع التبعيات
2. تشغيل السيرفر بشكل صحيح
3. استخدام الإصدار الصحيح من Node.js

---

**تم التطوير بـ ❤️ لمتداولي ICT**
