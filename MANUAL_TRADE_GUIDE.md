# دليل إدخال الصفقات يدوياً

## ✅ التغييرات التي تمت

### 1. تعطيل التحليل التلقائي
- تم تعطيل التحليل التلقائي كل 5 دقائق
- الكود موجود لكن معطل في `server/src/index.ts` (السطر 1957)

### 2. إضافة نظام الإدخال اليدوي
تم إنشاء:
- ✅ `server/src/routes/manualTrade.ts` - API endpoint للإدخال اليدوي
- ✅ `server/public/manual-trade.html` - واجهة ويب لإدخال الصفقات

---

## 📝 كيفية إدخال صفقة يدوياً

### الطريقة 1: عبر واجهة الويب (الأسهل)

1. افتح المتصفح واذهب إلى:
   ```
   http://localhost:3001/manual-trade.html
   ```
   أو في الإنتاج:
   ```
   https://your-domain.com/manual-trade.html
   ```

2. املأ البيانات:
   - نوع الصفقة (شراء/بيع)
   - سعر الدخول
   - وقف الخسارة
   - الأهداف الثلاثة (TP1, TP2, TP3)
   - التقييم والثقة (اختياري)
   - سبب الصفقة (اختياري)

3. اضغط "إرسال الصفقة"

4. سيتم إرسال الصفقة تلقائياً:
   - ✅ حفظ في قاعدة البيانات
   - ✅ إرسال عبر Telegram
   - ✅ إرسال عبر Push Notifications للتطبيق

---

### الطريقة 2: عبر API مباشرة

استخدم POST request إلى:
```
POST /api/manual-trade
```

**Headers:**
```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer YOUR_TOKEN"
}
```

**Body:**
```json
{
  "type": "BUY_LIMIT",
  "entry": 2650.50,
  "sl": 2645.00,
  "tp1": 2655.00,
  "tp2": 2660.00,
  "tp3": 2665.00,
  "score": 8,
  "confidence": 80,
  "reasoning": "سحب سيولة + BOS + FVG قريب"
}
```

**مثال باستخدام curl:**
```bash
curl -X POST http://localhost:3001/api/manual-trade \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "type": "BUY_LIMIT",
    "entry": 2650.50,
    "sl": 2645.00,
    "tp1": 2655.00,
    "tp2": 2660.00,
    "tp3": 2665.00,
    "score": 8,
    "confidence": 80,
    "reasoning": "صفقة يدوية"
  }'
```

---

## 🔄 إعادة تفعيل التحليل التلقائي

إذا أردت إعادة تفعيل التحليل التلقائي:

1. افتح ملف `server/src/index.ts`
2. اذهب إلى السطر 1957
3. أزل التعليق من السطر:
   ```typescript
   // scheduleNextAnalysis(); // ❌ معطل
   ```
   ليصبح:
   ```typescript
   scheduleNextAnalysis(); // ✅ مفعل
   ```
4. أعد تشغيل السيرفر

---

## 📊 ما يحدث عند إرسال صفقة يدوياً

1. ✅ يتم إنشاء كائن تحليل كامل (كأنه من AI)
2. ✅ يتم حفظ الصفقة في قاعدة البيانات
3. ✅ يتم إرسال إشعار Telegram لجميع المشتركين
4. ✅ يتم إرسال Push Notification لجميع أجهزة التطبيق
5. ✅ يظهر التحليل في التطبيق والموقع كأنه من AI

---

## 🎯 الشروط المطلوبة للصفقة

حسب التعليمات الجديدة في `aiService.ts`:

1. ✅ اتجاه H1 (صاعد → شراء، هابط → بيع)
2. ✅ سحب سيولة على M5 (خارجي أو داخلي)
3. ✅ BOS على M5 مع الاتجاه
4. ✅ منطقة دخول قريبة (FVG أو OB) - أقل من 1.5%
5. ✅ الأهداف محددة على سيولة أو فجوات

---

## 🔒 الأمان

- يجب أن يكون لديك token صالح للمصادقة
- في الإنتاج، استخدم نظام مصادقة قوي
- يمكنك إضافة middleware للتحقق من صلاحيات الإدخال اليدوي

---

## 📱 التطبيق

المستخدمون سيرون الصفقة في التطبيق تماماً كأنها من AI:
- نفس التنسيق
- نفس الإشعارات
- نفس البيانات

---

## ⚙️ ملاحظات تقنية

- الصفقات اليدوية تُحفظ بنوع `'manual'` في قاعدة البيانات
- يمكنك التمييز بينها وبين الصفقات التلقائية عند الحاجة
- التحليل التلقائي معطل لكن الكود موجود للرجوع إليه عند الحاجة
